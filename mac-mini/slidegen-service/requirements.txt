# CV pipeline
yt-dlp>=2024.1
opencv-python>=4.9
# Stage-1 PRIMARY extractor (ADR 0002 D1). Katna retired by the same decision.
scenedetect[opencv]>=0.6
pillow>=10.0
# Semantic routing VLM (local, Apple Silicon via MLX) — ADR 0001. Replaces CLIP.
# Apple-Silicon-only runtime; skipped on the Linux CI runner (tests mock it).
mlx-vlm>=0.1; sys_platform == "darwin"
# Document layout detection (graph/table region crop) — ADR 0001. Replaces YOLOv8.
doclayout-yolo>=0.0.2
# Equation OCR → LaTeX — ADR 0001. Replaces pix2tex. Fallback: PP-FormulaNet.
unimernet>=0.1
paddleocr>=2.7
matplotlib>=3.8
plotly>=5.18
realesrgan>=0.3

# API server
fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.27

# Storage
boto3>=1.34
supabase>=2.3
